{
  "name": "Rick and Morty GraphQL Schema",
  "schemaPath": "./src/gql/schema.graphql",
  "includes": ["**/*.gql", "**/*.graphql"],
  "extensions": {
    "endpoints": {
      "Default GraphQL Endpoint": {
        "url": "http://localhost:8080/graphql",
        "headers": {
          "user-agent": "JS GraphQL"
        },
        "introspect": false
      }
    }
  }
}